{
  "name": "fivem-redis",
  "version": "1.0.1",
  "main": "server/index.ts",
  "scripts": {
    "build": "npm run clearbuild && npm run typecheck && node _build/bundle.js",
    "clearbuild": "rimraf ./dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@citizenfx/server": "^2.0.10822-1",
    "@types/node": "^22.9.0",
    "esbuild": "^0.24.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
